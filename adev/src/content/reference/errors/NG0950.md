# 設定值之前存取所需的輸入。

@description
存取所需的輸入，但未繫結任何值。

這可能會在指令或元件中過早存取所需輸入時發生。
這通常在將輸入視為類別結構的一部分時發生。

輸入保證在 `ngOnInit` 生命週期掛鉤和之後可用。

## 修正錯誤

在響應式內容中存取所需的輸入。
例如，在範本本身、在 `computed` 內部或在效果內部。

或者，在 `ngOnInit` 生命週期掛鉤內或稍後存取輸入。
