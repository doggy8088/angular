# 缺少 Iterable Differ

`NgFor` 無法為傳入的值找到可叠代差異。請確保它是一個可叠代對象，如 `Array`。

## 偵錯錯誤

當在範本中使用 ngFor 時，您必須使用某種 Iterable，例如 `Array`、`Set`、`Map` 等。
如果您嘗試遍歷物件中的鍵，您應該改看 [KeyValue pipe](/api/common/KeyValuePipe)。

